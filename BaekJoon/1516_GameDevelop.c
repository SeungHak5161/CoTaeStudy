#include <stdio.h>

typedef int bool;
enum { false, true };

struct node {
	bool visit;
	int length;
	int parent;
};

int main() {
	int num;
	scanf("%d", &num);

	//int *arr;
	//(int*)arr = malloc(sizeof(int)*num);

	for (int i = 0; i < num; i++) {
		scanf("%d %d", &node.length);
	}
}